{
  "name": "mcryptoex",
  "private": true,
  "version": "0.1.0",
  "description": "mCryptoEx monorepo",
  "scripts": {
    "dev": "docker compose up --build",
    "dev:down": "docker compose down -v",
    "dev:ps": "docker compose ps",
    "dev:logs": "docker compose logs -f",
    "lint": "echo 'Phase 1: lint targets will be added per workspace in later movements'",
    "test": "echo 'Phase 1: tests are introduced from Movement 2 onward'"
  }
}
